{
  "name": "mtg-draft-overlay",
  "version": "0.1.0",
  "private": true,
  "description": "Transparent, click-through, always-on-top draft overlay for MTG Arena — renders the current pack from mtg-draft's current.json with 17Lands GIH, CGB grade, wheel flags, tags, and the pre-baked AI take.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && cp src/index.html src/style.css dist/",
    "start": "npm run build && electron .",
    "probe": "tsx src/probe.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "electron": "^31.0.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
